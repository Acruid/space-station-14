#version 330 core

layout (location = 0) in vec3 aVert;
layout (location = 1) in vec4 aPos;
layout (location = 2) in vec4 aColor;

out vec4 ourColor;

uniform mat4 transform;

void main()
{
    gl_Position = transform * vec4(aVert * aPos.w + aPos.xyz, 1.0);
    ourColor = aColor;
}
